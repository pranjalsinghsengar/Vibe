version: "3.8"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/src/app
      - /src/app/node_modules
    ports:
      - "5050:5050"
    command: npm start