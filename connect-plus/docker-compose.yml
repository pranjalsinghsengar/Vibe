

services:
  app:
    build: .
    ports:
      - "3014:3014"
    environment:
      NODE_ENV: production
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    restart: unless-stopped
